C++ data structures
Using heaps and hash tables for efficient basic functions such as lookups. to then use them in a complex graph system for finding shortest paths, communities, minimum effort paths, critical nodes, echo chambers etc.
The test files were proviced by the course staff.
To run: "make test"
